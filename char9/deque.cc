#include <deque>
#include <list>
#include <iostream>
#include <string>

using namespace std;

int main(int argc, char const *argv[])
{
    string word;
    // deque<string> deq;
    // while (cin>>word)
    // {
    //     deq.push_back(word);
    // }

    // for(const string& s:deq) {
    //     cout << s << endl;
    // }

    // list<string> l;
    // while (cin>>word)
    // {
    //     l.push_back(word);
    // }

    // for(const string& s:l) {
    //     cout << s << endl;
    // }

    // list<int> l;
    // int i;
    // while(cin>>i) {
    //     l.push_back(i);
    // }

    // deque<int> odd;
    // deque<int> even;
    // for(const int& i : l) {
    //     if (i%2==0) {
    //         even.push_back(i);
    //     } else {
    //         odd.push_back(i);
    //     }
    // }
    
    return 0;
}

